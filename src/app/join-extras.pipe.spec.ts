import { JoinExtrasPipe } from './join-extras.pipe';

describe('JoinExtrasPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinExtrasPipe();
    expect(pipe).toBeTruthy();
  });
});
